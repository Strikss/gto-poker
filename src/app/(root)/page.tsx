// Drag & drop removed from the home screen for now

export const revalidate = 0;

export default async function Home() {
  return (
    <section className="flex min-h-[60vh] flex-col items-center justify-center gap-4"></section>
  );
}
